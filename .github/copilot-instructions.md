# Copilot Instructions

## Project Guidelines
- Project rule: when creating/updating users in admin panel, Admin names must end with 'Admin' and Trainer names must include 'Trainer'.
- Ensure all database changes are compatible with SQL Server, as managed via SQL Server Management Studio (SSMS).

## Design Preferences
- Use a real background picture or illustration instead of sticker or emoji decorations on authentication pages.