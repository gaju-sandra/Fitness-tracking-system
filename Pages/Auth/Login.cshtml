@page
@model fit.Pages.Auth.LoginModel
@{ Layout = null; }
<!DOCTYPE html>
<html lang="en" data-theme="light">
<head>
<meta charset="utf-8"/>
<meta name="viewport" content="width=device-width,initial-scale=1"/>
<title>Login – FitTrack</title>
<link rel="icon" type="image/svg+xml" href="/favicon.svg?v=2"/>
<link rel="shortcut icon" href="/favicon.svg?v=2"/>
<link rel="stylesheet" href="~/css/fittrack.css"/>
</head>
<body class="auth-body auth-screen">
<div class="auth-visual" aria-hidden="true">
  <img src="/images/auth-fitness-bg.svg" class="auth-visual-image" alt=""/>
  <div class="auth-orb orb-1"></div>
  <div class="auth-orb orb-2"></div>
  <div class="auth-orb orb-3"></div>
</div>

<main class="auth-page">
  <div class="auth-card auth-card-premium">
    <div class="auth-logo">FitTrack</div>
    <h2>Welcome Back</h2>
    <p class="text-muted mb-4">Sign in to your fitness journey</p>
    @if (Model.Error != null)
    {
      <div class="alert alert-danger">@Model.Error</div>
    }
    <form method="post">
      <div class="form-group">
        <label>Email</label>
        <input type="email" name="Email" class="input" placeholder="you@example.com" required/>
      </div>
      <div class="form-group">
        <label>Password</label>
        <input type="password" name="Password" class="input" placeholder="••••••••" required/>
      </div>
      <button type="submit" class="btn btn-primary auth-submit">Sign In</button>
    </form>
    <p class="auth-links">No account? <a href="/Auth/Register">Register here</a></p>
    <p class="auth-links"><a href="/">← Back to Home</a></p>
    <div class="demo-creds">
      <small><strong>Demo:</strong> admin@fittrack.rw / Admin@123<br/>
      trainer@fittrack.rw / Trainer@123<br/>
      alice@fittrack.rw / User@123</small>
    </div>
  </div>
</main>

<footer class="auth-footer">
  <span>© @DateTime.UtcNow.Year FitTrack</span>
  <span>•</span>
  <span>Stronger every day</span>
</footer>
<script src="~/js/fittrack.js"></script>
</body>
</html>
